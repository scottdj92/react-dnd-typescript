import React from "react";
import styled from "@emotion/styled";
import { DragSourceSpec, DragSourceCollector } from "react-dnd";

export const File: React.FC = () => (
    <span>I'm a file, Morty</span>
);
